openapi: 3.2.0
info:
  description: API documentation of Admin Portal - Admin Management
  title: Admin Portal - Admin Management Admin Notification…
  version: v0.0.1
servers:
- url: https://{tenant}
  variables:
    tenant:
      default: example.strivacity.com
      description: Your Strivacity tenant
tags:
- name: Admin Notification Preference Descriptor
paths:
  /admin/api/v1/admin/notificationPreferences/descriptor:
    get:
      description: You can list the possible descriptors for admin notification preferences by calling this endpoint.
      operationId: getNotificationPreferencesDescriptor
      responses:
        '200':
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DisplayNameAndIdResponse'
          description: OK
      summary: List descriptors for admin notification preferences
      tags:
      - Admin Notification Preference Descriptor
components:
  schemas:
    DisplayNameAndIdResponse:
      type: object
      properties:
        displayName:
          type: string
        id:
          type: string
  securitySchemes:
    securityScheme:
      scheme: bearer
      type: http
